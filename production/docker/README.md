# Docker / Compose (planned) — root

Contents to add:

- `docker-compose.yml` examples for local integration testing
- `Dockerfile` best-practices for backend and frontend services
- Build and publish helpers for registry (ECR/ACR)

Quick example

```bash
docker-compose -f future-deploy/docker/docker-compose.yml up --build
```

This folder is for small, deterministic container deployments and CI build helpers.
