model Marketplace {
  id           String    @id @default(cuid())
  userId       String
  title        String
  description  String
  price        Float
  mainCategory String
  category     String[]
  subcategory  String[]
  condition    String?
  categoryTag  String?
  region       String
  city         String
  images       String[]
  extra        Json?
  maGaday      Boolean   @default(false)
 isBasic30    Boolean   @default(false)
  isStandard60 Boolean   @default(false)
  isPremium90  Boolean   @default(false)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  isPaid       Boolean   @default(false)
  expiryDate   DateTime?
  feeId        String?   @unique
  feeAmount    Float     @default(0)
  planId       String?
  planAmount   Float     @default(0)
  
  user         User      @relation("UserMarketplaces", fields: [userId], references: [id], onDelete: Cascade)
  payments     Payment[] @relation("MarketplacePayments")
  chats        Chat[]
  messages     Message[] @relation("MarketplaceToMessage")
  fee          MarketplaceFee? @relation(fields: [feeId], references: [id])
  plan         SubPlan?  @relation(fields: [planId], references: [id])

  @@index([isPaid])
  @@index([createdAt])
  @@index([userId])
  @@index([region])
}

model RealEstate {
  id           String    @id @default(cuid())
  userId       String
  title        String
  description  String
  price        Float
  mainCategory String
  category     String[]
  subcategory  String[]
  propertyType String?
  bedrooms     Int?
  bathrooms    Int?
  floor        Int?
  totalFloors  Int?
  squareFeet   Int?
  sizeSqm      Float?
  furnished    Boolean?
  parking      Boolean?
  amenities    String[]
  address      String
  hasGarage    Boolean?
  hasGarden    Boolean?
  region       String
  city         String
  county       String
  images       String[]
  maGaday      Boolean   @default(false)
  isBasic30    Boolean   @default(false)
  isStandard60 Boolean   @default(false)
  isPremium90  Boolean   @default(false)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  isPaid       Boolean   @default(false)
  expiryDate   DateTime?
  feeId        String?   @unique
  feeAmount    Float     @default(0)
  planId       String?
  planAmount   Float     @default(0)
  
  user         User      @relation("UserRealEstates", fields: [userId], references: [id], onDelete: Cascade)
  payments     Payment[] @relation("RealEstatePayments")
  chats        Chat[]
  messages     Message[] @relation("MessageToRealEstate")
  fee          RealEstateFee? @relation(fields: [feeId], references: [id])
  plan         SubPlan?  @relation(fields: [planId], references: [id])

  @@index([isPaid])
  @@index([createdAt])
  @@index([userId])
  @@index([region])
}

model Boat {
  id           String    @id @default(cuid())
  userId       String
  title        String
  mainCategory String
  category     String[]
  subcategory  String[]
  region       String
  city         String
  description  String
  price        Float
  images       String[]
  type         String
  boatModel    String
  transmission String
  color        String
  maGaday      Boolean   @default(false)
  isBasic30    Boolean   @default(false)
  isStandard60 Boolean   @default(false)
  isPremium90  Boolean   @default(false)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  isPaid       Boolean   @default(false)
  expiryDate   DateTime?
  feeId        String?   @unique
  feeAmount    Float     @default(0)
  planId       String?
  planAmount   Float     @default(0)
  
  user         User      @relation("UserBoats", fields: [userId], references: [id], onDelete: Cascade)
  payments     Payment[] @relation("BoatPayments")
  chats        Chat[]
  fee          BoatFee?  @relation(fields: [feeId], references: [id])
  plan         SubPlan?  @relation(fields: [planId], references: [id])

  @@index([isPaid])
  @@index([createdAt])
  @@index([userId])
  @@index([region])
}

model Car {
  id           String    @id @default(cuid())
  userId       String
  title        String
  description  String
  price        Float
  mainCategory String
  category     String[]
  subcategory  String[]
  brand        String
  vehicleModel String
  make         String?
  model        String?
  trim         String?
  year         Int?
  mileage      Int?
  transmission String?
  gearbox      String?
  fuelType     String?
  engineSize   String?
  condition    String?
  color        String
  doors        Int?
  region       String
  city         String
  images       String[]
  maGaday      Boolean   @default(false)
  isBasic30    Boolean   @default(false)
  isStandard60 Boolean   @default(false)
  isPremium90  Boolean   @default(false)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  isPaid       Boolean   @default(false)
  expiryDate   DateTime?
  feeId        String?   @unique
  feeAmount    Float     @default(0)
  planId       String?
  planAmount   Float     @default(0)
  
  user         User      @relation("UserCars", fields: [userId], references: [id], onDelete: Cascade)
  payments     Payment[] @relation("CarPayments")
  chats        Chat[]
  fee          CarFee?   @relation(fields: [feeId], references: [id])
  plan         SubPlan?  @relation(fields: [planId], references: [id])

  @@index([isPaid])
  @@index([createdAt])
  @@index([userId])
  @@index([region])
}

model Motorcycle {
  id           String    @id @default(cuid())
  userId       String
  title        String
  transmission String?
  gearbox      String?
  mainCategory String
  category     String[]
  subcategory  String[]
  price        Float
  region       String
  city         String
  images       String[]
  type         String
  make         String
  modelName    String
  model        String?
  year         Int
  mileage      Int
  engineSize   String
  engineCc     String?
  fuelType     String
  color        String
  condition    String?
  description  String
  maGaday      Boolean   @default(false)
  isBasic30    Boolean   @default(false)
  isStandard60 Boolean   @default(false)
  isPremium90  Boolean   @default(false)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  isPaid       Boolean   @default(false)
  expiryDate   DateTime?
  feeId        String?   @unique
  feeAmount    Float     @default(0)
  planId       String?
  planAmount   Float     @default(0)
  
  user         User           @relation("UserMotorcycles", fields: [userId], references: [id], onDelete: Cascade)
  payments     Payment[]      @relation("MotorcyclePayments")
  chats        Chat[]
  fee          MotorcycleFee? @relation(fields: [feeId], references: [id])
  plan         SubPlan?       @relation(fields: [planId], references: [id])

  @@index([isPaid])
  @@index([createdAt])
  @@index([userId])
  @@index([region])
}

model Farmequipment {
  id                  String    @id @default(cuid())
  userId              String
  title               String
  description         String
  price               Float
  mainCategory        String
  category            String[]
  subcategory         String[]
  type                String
  equipmentType       String?
  make                String
  brand               String?
  farmequipmentModel  String
  year                Int
  condition           String
  hours               Int?
  hoursUsed           Int?
  lastServiceDate     DateTime?
  attachmentsIncluded Boolean?
  enginePower         String
  fuelType            String
  region              String
  city                String
  images              String[]
  maGaday       Boolean   @default(false)
  isBasic30     Boolean   @default(false)
  isStandard60  Boolean   @default(false)
  isPremium90   Boolean   @default(false)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  isPaid        Boolean   @default(false)
  expiryDate    DateTime?
  feeId         String?   @unique
  feeAmount     Float     @default(0)
  planId        String?
  planAmount    Float     @default(0)
  
  user          User      @relation("UserFarmequipments", fields: [userId], references: [id], onDelete: Cascade)
  payments      Payment[] @relation("FarmPayments")
  chats         Chat[]
  fee           EquipmentFeeConfig? @relation(fields: [feeId], references: [id])
  plan          SubPlan?  @relation(fields: [planId], references: [id])

  @@index([isPaid])
  @@index([createdAt])
  @@index([userId])
  @@index([region])
}

model Job {
  id              String        @id @default(cuid())
  userId          String
  title           String
  description     String
  salary          Float
  mainCategory    String
  category        String[]
  subcategory     String[]
  region          String
  city            String
  employmentType  String
  experienceLevel String
  images          String[]
  maGaday         Boolean       @default(false)
  isBasic30       Boolean       @default(false)
  isStandard60    Boolean       @default(false)
  isPremium90     Boolean       @default(false)
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  isPaid          Boolean       @default(false)
  expiryDate      DateTime?
  feeId           String?       @unique
  feeAmount       Float         @default(0)
  planId          String?
  planAmount      Float         @default(0)
  
  user            User          @relation("UserJobs", fields: [userId], references: [id], onDelete: Cascade)
  payments        Payment[]     @relation("JobPayments")
  chats           Chat[]
  fee             JobFeeConfig? @relation("JobToJobFee", fields: [feeId], references: [id])
  plan            SubPlan?      @relation("JobToSubPlan", fields: [planId], references: [id])

  @@index([isPaid])
  @@index([createdAt])
  @@index([userId])
  @@index([region])
}