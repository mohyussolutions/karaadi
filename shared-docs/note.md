1. Backend
   Implement i18n (translation)
   User management:
   Self-delete (user) add IAM in the db
2. Frontend
   Move sorting/filtering to backend

   Complete category pages (e.g. boats)
   Fix chat UI logic
   Refactor actions into 3 components
   Add form validation
   Fix TypeScript types
   Final build + optimization
   Integrate payment flow + protect routes
   Show 90-day plan users first
   Integrate HageAI
   ⚙️ Auth / UI Issues
   Fix redirect loop on refresh
   Keep DashboardLayout mounted
   Cache auth state (avoid null flash)
   Use cookies (recommended) for persistence
   Ensure layout stays static, only children refresh

3. Deployment / AWS
   Deploy to AWS production
   Set up monitoring (CloudWatch)
   Finalize payments
   i18n setup

Stack:
Amplify, EC2, RDS, S3 + CloudFront, Cognito, Route 53, ElastiCache
